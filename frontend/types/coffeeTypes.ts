export type CoffeeItem = {
  id: string;
  date_created: string;
  name: string;
  price: number;
  region: string;
  roastLevel: string;
  status: string;
};

export type Filter = {
  region: string;
};
